<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <% include ./partials/header %>
    <title>Friends</title>
</head>

<body>
    <% include ./partials/nav %>

    <form action="/editMessage/:id" method="POST">
        <input type="hidden" name="_id" value="<%= messages._id %>">
        <input type="text" name="text" value="<%= messages.text %>">
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>

    <%  include ./partials/scripts %>
</body>

</html>